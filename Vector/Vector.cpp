#include <iostream>
#include <vector>
using namespace std;

int main(){
    vector<int>elements;

    elements.push_back(7);
    elements.push_back(6);
    elements.push_back(5);
    elements.push_back(4);

    cout<<elements.size();
}